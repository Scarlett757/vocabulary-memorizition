# 单词背诵软件 · 产品介绍

> 一个面向中国学生的小、初、高、大学、雅思全阶段英语词汇记忆与背诵系统。
> 基于 Flask + MySQL + Docker 部署，集成 edge-tts 发音、Ollama 本地大模型翻译、ECDICT 全量词库、人教版初中词汇。

## 一、产品定位

面向 K12 与大学阶段英语学习者，提供「词库分层 → 今日背诵 → 卡片记忆 → 间隔复习 → 打卡激励」的完整闭环。区别于百词斩、扇贝等商业产品的"账号驱动 + 广告 + 增值订阅"模式，本项目聚焦**个人学习路径自定义**与**离线可用性**：所有核心功能可在一台 Docker 容器内独立运行，无需依赖第三方 SaaS。

## 二、目标用户

| 角色 | 痛点 | 本项目解决方案 |
|------|------|----------------|
| 中学生 | 教材词汇零散、不知道每天该背哪些 | 按人教版初中/小学词库分层，自动生成每日新词计划 |
| 四六级考生 | 词库太泛、背了不会用 | 内置四级/六级/托福/雅思/GRE/考研分层，按词频排序 |
| 自学者 | 缺乏发音、释义、复习节奏 | edge-tts 美音/英音双声道 + Ollama 自动翻译 + 30 天间隔重复 |

## 三、核心功能模块

### 1. 词汇本分层

- 内置 7 级词库：小学 / 初中（人教版 2895 词）/ 高中 / 四级 / 六级 / 托福 / 雅思 / GRE / 考研
- 初中再细分初一 / 初二 / 初三，覆盖人教版教材全部词汇
- 支持自选词汇本：用户可从 ECDICT 77 万词库中搜索添加，或手动录入新词
- 单词书界面显示已熟悉 / 学习中 / 未学的整体进度条

### 2. 今日背诵引擎

- **持久化今日计划**：当天首次访问生成"新词 + 复习词 + 手动添加词"列表并入库，背完一些后不再自动补词，只有用户显式"继续背 N 个"才追加
- **点 ✓ 即移除**：当天点过对勾的词从今日列表和背诵界面同时移除，重新进入不再显示
- **点 ✗ 推到本轮末尾**：第一次点叉的词放到本轮最后，必须全部点过 ✓ 才能完成
- **每日进度限制**：一个单词每天只能 +1/3 进度或清零，防止刷熟练
- **今日忘记视图**：单独列出当天点过忘记的词，可单独背诵、单独恢复进度

### 3. 卡片记忆界面

- 翻转卡片：正面单词 + 音标，背面中文释义
- 美音 / 英音双声道发音（edge-tts 神经网络音色，按需生成、本地缓存）
- 翻页提示音（Web Audio API 生成，不依赖音频文件）
- 背诵列表为空时显示"去今日背诵添加单词"引导卡片

### 4. 间隔重复复习

- 熟悉判定：连续 3 次答对 → 标记为熟悉，30 天后重新出现
- 复习触发：`next_review_at <= now` 或 learning 状态且昨天及之前背过
- 忘记操作：一键重置进度为 0，状态回退为 learning

### 5. 句子背诵

- 支持用户添加英汉对照句子，中文留空时自动调用 **Ollama qwen2.5:7b** 本地大模型翻译
- 句子卡片支持**先中文后英文 / 先英文后中文**双方向背诵
- 句子内容支持**字体自适应**：短句大字、长文（最多 500 字）小字 + 滚动条
- 句子背诵完成逻辑与单词一致：点 ✗ 推到本轮末尾

### 6. 自定义分组

- 用户可创建任意分组（如"收藏"、"单元一"、"错题本"）
- 单词和句子均可加入分组，支持批量勾选 + 批量操作
- 分组可单独进入背诵模式，背完可触发分组打卡

### 7. 打卡系统

- 本月月历视图，已打卡日盖章 ✓
- 连续打卡天数统计（🔥 streak）
- 当日完成单词数 / 句子数统计
- 两种打卡目标：①完成今日新词 + 复习目标 ②完成指定分组背诵

### 8. 设置与主题

- 每日新词数量（5~100 可调）
- 熟悉判定阈值（默认 3 次连续答对）
- 默认发音口音（US / UK）
- 深浅主题切换，状态 localStorage 持久化
- 词汇本层级气泡选择器：点"初中"再浮出初一 / 初二 / 初三

## 四、技术架构

```
┌─────────────────────────────────────────────┐
│  浏览器（Jinja2 模板 + 原生 JS + CSS 变量）  │
└──────────────────────┬──────────────────────┘
                       │ HTTP
┌──────────────────────▼──────────────────────┐
│  Flask 应用（app.py 单文件路由）              │
│  - SQLAlchemy ORM  - Flask session           │
│  - smtplib 邮件验证码  - edge-tts 按需 TTS   │
│  - requests 调用 Ollama                      │
└──────┬───────────────┬───────────────┬─────┘
       │               │               │
┌──────▼──────┐  ┌─────▼─────┐  ┌──────▼──────┐
│  MySQL 8.0  │  │  Ollama   │  │  edge-tts   │
│  (词库+进度) │  │ qwen2.5:7b│  │  (发音缓存)  │
└─────────────┘  └───────────┘  └─────────────┘
```

### 技术选型理由

| 选型 | 理由 |
|------|------|
| Flask + Jinja2 | 单文件路由、快速迭代、无需前后端分离的复杂度 |
| MySQL + SQLAlchemy | 关系型存储用户/词库/进度/分组；ORM 抽象便于切换数据库 |
| edge-tts | 免费、神经网络音色、美英双声道；按需生成避免预下载 77 万音频 |
| Ollama + qwen2.5:7b | 本地推理、无 API 费用、无数据外泄、Docker 网络内可达 |
| ECDICT | 开源英汉词典 77 万词条，覆盖词频、音标、释义 |
| Docker Compose | 一键部署 app + mysql + ollama 三服务，环境隔离 |

## 五、数据规模

| 数据 | 数量 |
|------|------|
| ECDICT 全量词库 | 770,607 词 |
| 人教版初中词汇 | 2,895 词 |
| 小学 / 高中 / 四级 / 六级 / 托福 / 雅思 / GRE / 考研 | 按层级覆盖 |
| 数据表 | 10 张（users / words / user_word_progress / user_sentences / user_groups / user_group_items / today_plan / today_extras / email_codes / checkins） |

## 六、工程实践亮点

1. **敏感凭据零硬编码**：所有密码、邮箱授权码、SECRET_KEY 通过 `.env` 文件或环境变量注入，`.env` 被 `.gitignore` 和 `.dockerignore` 双重排除
2. **TodayPlan 持久化**：避免了"背完 30 个词重新进入又补 30 个"的体验问题
3. **点 ✓ 即移除 + 每日进度上限**：防止用户通过反复点 ✓ 刷熟练度
4. **字体自适应 + 滚动卡片**：500 字长句也能在固定高度卡片内完整阅读
5. **翻页提示音**：Web Audio API 生成，不依赖音频文件，零额外资源
6. **Docker 一键部署**：`docker compose up -d` 启动 app + mysql + ollama，数据卷持久化

## 七、部署方式

```bash
# 1. 复制环境变量模板并填入真实凭据
cp .env.example .env
vim .env

# 2. 一键启动
docker compose up -d

# 3. 访问
open http://localhost:5000
```

## 八、适合岗位

- Python 后端开发（Flask / SQLAlchemy / smtplib / REST API 设计）
- 全栈开发（Jinja2 模板 + 原生 JS + CSS 变量主题系统）
- DevOps 实践（Docker / docker-compose / 环境变量管理 / 数据卷持久化）
- AI 工程落地（Ollama 本地大模型集成 / edge-tts 神经网络 TTS）
- 产品思维（用户路径设计 / 数据驱动分层 / 间隔重复算法）
